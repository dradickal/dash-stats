import type { Lists } from '.keystone/types';

import { Dash } from './schema/Dash';
import { User } from './schema/User';
import { Zone } from './schema/Zone';

export const lists: Lists = {
  User,
  Dash,
  Zone,
  
};
